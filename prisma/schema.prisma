// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}


datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// enum Role {
//   admin
//   user
//   client
// }

model User {
  id        String    @id @default(uuid())
  email     String   @unique
  emailVerified DateTime?
  password  String
  name      String
  numLogs   Int      @default(0)
  isAdmin   Boolean  @default(false)
  isActive  Boolean  @default(true)
  // role      Role   @default(user)
  role      String   @default("user")
  roles     String[] @default(["user"])
  address UserAddress?
  order  Order[]
  receipt  Receipt[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}


model Country {
  id   String @id
  name String

  UserAddress  UserAddress[]
  OrderAddress OrderAddress[]
}

model UserAddress {
  id         String  @id @default(uuid())
  firstName  String
  lastName   String
  address    String
  address2   String?
  postalCode String
  phone      String
  city       String

  // Relaciones
  country   Country @relation(fields: [countryId], references: [id])
  countryId String

  user String @unique
  user1   User   @relation(fields: [user], references: [id])
}


model Customer {
  id        String    @id @default(uuid())
  codCus    String   @unique
  nameCus   String   @unique
  emailCus  String?
  domcomer  String?
  cuit      String?
  coniva    String?
  order  Order[]   // 👈 Relación 
  receipt  Receipt[]   

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Supplier {
  id            String    @id @default(uuid())
  codSup    String   @unique
  name      String   @unique
  email     String?
  domcomer  String?
  cuit      String?
  coniva    String?
  products   Product[]
  order  Order[]   // 👈 Relación 
  receipt  Receipt[]   

  updatedAt DateTime @updatedAt
}

model Valuee {
  id            String    @id @default(uuid())
  codVal        String   @unique
  desVal        String   @unique
  receiptItem  ReceiptItem[] 

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}
model StateOrd {
  id            String    @id @default(uuid())
  name          String   
  note          String   
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}
model Encargado {
  id            String    @id @default(uuid())
  codEnc        String   @unique
  name          String   @unique
  email         String?   
  receipt  Receipt[]   

  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model Comprobante {
  id            String    @id @default(uuid())
  codComC    String?
  nameCom    String?
  claCom     String?
  isHaber    Boolean       @default(true)
  noDisc     Boolean       @default(true)
  toDisc     Boolean       @default(false)
  itDisc     Boolean       @default(false)
  interno    Boolean       @default(false)
  numInt     Int?
  
  codConId    String?         // 👈 Debe ser STRING para coincidir con el UUID del Configuration.id
  codCon      Configuration?  @relation(fields: [codConId], references: [id], onDelete: Restrict)
  order  Order[]   // 👈 Relación 
  
  createdAt  DateTime      @default(now())
  updatedAt  DateTime      @updatedAt

  @@unique([codComC, codConId])
}

model Parte {
  id        String   @id @default(uuid())
  codPar    String   @unique
  name      String   @unique
  email     String?
  domcomer  String?
  cuit      String?
  coniva    String?
  order  Order[]   // 👈 Relación con OrderItem
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}


model Configuration {
  id            String    @id @default(uuid())
  codCon      String   @unique
  name        String   @unique
  domcomer    String?
  cuit        String?
  coniva      String?
  ib          String?
  feciniact   String?   // mejor que string → Prisma/Postgres maneja fechas nativas
  numIntRem   Int      @default(0)
  numIntRec   Int      @default(0)
  numIntOdp   Int      @default(0)
  numIntCaj   Int      @default(0)
  numIntMov   Int      @default(0)
  numIntCli   Int      @default(0)
  orders1       Order[]     @relation("Config1")
  orders2       Order[]     @relation("Config2")
  receipt  Receipt[]   
  comprobantes Comprobante[]
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
}

/////
model Instrumento {
  id          String      @id @default(uuid())
  codIns      String
  name        String
  orderItems OrderItem[]
  order  Order[]   // 👈 Relación con OrderItem
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
}


model Category {
  id      String    @id @default(uuid())
  name    String    @unique
  Product Product[]
}

model Product {
  id          String   @id @default(uuid())
  codPro      String
  codigoPro   String
  title       String
  medPro      String
  slug        String?
  image       String?
  images      String[]
  image1      String?
  image2      String?
  image3      String?
  brand       String?
  category    String?
  id_config   String?
  // supplier    String?
  id_category String?

  categorys   Category @relation(fields: [categoryId], references: [id])
  categoryId String

  ecoActive   Boolean       @default(true)
  description String
  price       Float?
  priceBuy    Float?
  inStock     Float?
  minStock    Float?
  porIva      Float?
  rating      Float?
  numReviews  Int?
  tags        String[]
  type        ProductType?
  gender      Gender?
  sizes       String[]
  reviews     Review[]
  orderItems  OrderItem[]   // 👈 Relación con OrderItem
  ProductImage ProductImage[]

  supplierId    String?         // 👈 Debe ser STRING para coincidir con el UUID del Configuration.id
  supplier      Supplier?   @relation(fields: [supplierId], references: [id], onDelete: Restrict)
  createdAt   DateTime    @default(now())
  updatedAt   DateTime    @updatedAt
  @@unique([codigoPro, id_config])
}

model ProductImage {
  // id  Int    @id @default(autoincrement())
  id        String  @id @default(uuid())

  url String

  product   Product @relation(fields: [productId], references: [id], onDelete: Restrict)
  productId String
}

model Review {
  id        String  @id @default(uuid())
  productId String
  name      String?
  comment   String?
  rating    Float?
  product   Product @relation(fields: [productId], references: [id], onDelete: Restrict)
}

enum ProductType {
  shirts
  pants
  hoodies
  hats
}

enum Gender {
  men
  women
  kid
  unisex
}


model OrderItem {
  id          String   @id @default(uuid())
  slug        String?
  title       String
  medPro      String
  quantity    Int
  image       String?
  price       Float
  size        String?
  porIva      Float
  venDat      DateTime?
  observ      String?
  terminado   Boolean?
  productId   String
  product     Product   @relation(fields: [productId], references: [id], onDelete: Restrict)
  instrumento Instrumento? @relation(fields: [instrumentoId], references: [id], onDelete: Restrict)
  instrumentoId String?
  order Order? @relation(fields: [orderId], references: [id], onDelete: Restrict)
  orderId String?
}


model OrderAddress {
  id         String  @id @default(uuid())
  firstName  String
  lastName   String
  address    String
  address2   String?
  postalCode String
  city       String
  phone      String

  // Relaciones
  country   Country @relation(fields: [countryId], references: [id])
  countryId String

  Order   Order  @relation(fields: [orderId], references: [id])
  orderId String @unique
}

model Order {
  id             String       @id @default(uuid())
  orderItems     OrderItem[]  // relación 1:N con OrderItem
  OrderAddress OrderAddress?
  shippingAddress ShippingAddress[]
  paymentMethod  String?
  paymentResult  PaymentResult?

  numberOfItems  Int?
  itemsInOrder  Int?
  subTotal       Float?
  shippingPrice  Float?
  tax            Float?
  total          Float?
  totalBuy       Float?

  id_client      String?      // relación con Customer
  customer     Customer?   @relation(fields: [id_client], references: [id], onDelete: Restrict)
  supplier      String?      // relación con Customer
  supplier1     Supplier?   @relation(fields: [supplier], references: [id], onDelete: Restrict)
  id_parte       String?      // relación con Parte
  parte        Parte?   @relation(fields: [id_parte], references: [id], onDelete: Restrict)
  id_instru      String?      // relación con Instrumento
  instrumento  Instrumento?   @relation(fields: [id_instru], references: [id], onDelete: Restrict)

  id_config       String?
  configuration   Configuration?  @relation("Config1", fields: [id_config], references: [id], onDelete: Restrict)

  id_config2      String?
  configuration2  Configuration?  @relation("Config2", fields: [id_config2], references: [id], onDelete: Restrict)

  codConNum      String?
  codCom         String?
  comprobante  Comprobante?   @relation(fields: [codCom], references: [id], onDelete: Restrict)

  libNum         Int?
  folNum         Int?
  asiNum         Int?
  asiDat         DateTime?
  escNum         Int?
  asieNum        Int?
  asieDat        DateTime?

  terminado      Boolean      @default(false)

  isHaber        Boolean?
  user           String?
  user1  User?   @relation(fields: [user], references: [id], onDelete: Restrict)
  id_delivery    String?
  id_address     String?

  isPaid         Boolean      @default(false)
  paidAt         DateTime?
  isDelivered    Boolean      @default(false)
  deliveredAt    DateTime?

  remNum         Int?
  remDat         DateTime?
  dueDat         DateTime?
  movpvNum       Int?
  movpvDat       DateTime?
  invNum         Int?
  invDat         DateTime?
  recNum         Int?
  recDat         DateTime?

  desVal         String?
  ordNum         Int?
  notes          String?
  salbuy         String?
  pedcotNum      Int?
  pedcotDat      DateTime?
  cotNum         Int?
  cotDat         DateTime?
  ordYes         String?
  staOrd         String?
  status         String?

  lat            Float?
  lng            Float?

  createdAt      DateTime     @default(now())
  updatedAt      DateTime     @updatedAt
}

model ShippingAddress {
  id             String  @id @default(uuid())
  fullName       String?
  firstName      String?
  lastName       String?
  address        String?
  address2       String?
  city           String?
  postalCode     String?
  zip            String?
  country        String?
  cuit           String?
  phone          String?
  orderId        String  @unique
  order          Order   @relation(fields: [orderId], references: [id])
}

model PaymentResult {
  id            String   @id @default(uuid())
  status        String?
  update_time   String?
  email_address String?
  orderId       String   @unique
  order         Order    @relation(fields: [orderId], references: [id])
}


model Receipt {
  id             String          @id @default(uuid())
  receiptItems   ReceiptItem[]   // relación 1:N
  subTotal       Float?
  total          Float?
  totalBuy       Float?
  codConNum      Int?
  isPaid         Boolean?        @default(false)
  paidAt         DateTime?
  recNum         Int?
  recDat         DateTime?
  cajNum         Int?
  cajDat         DateTime?
  desval         String?
  ordNum         Int?
  notes          String?
  salbuy         String?

  // Relaciones
  id_client    String? 
  customer      Customer?       @relation(fields: [id_client], references: [id], onDelete: Restrict)

  id_config    String?
  configuration      Configuration?  @relation(fields: [id_config], references: [id], onDelete: Restrict)

  id_encarg    String?
  encargado      Encargado?      @relation(fields: [id_encarg], references: [id], onDelete: Restrict)

  user         String?
  user1           User?           @relation(fields: [user], references: [id], onDelete: Restrict)

  supplier     String?
  supplier1       Supplier?       @relation(fields: [supplier], references: [id], onDelete: Restrict)

  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
}

model ReceiptItem {
  id          String   @id @default(uuid())
  desval      String
  numval      Float?
  amountval   Float
  receiptId   String
  receipt     Receipt  @relation(fields: [receiptId], references: [id], onDelete: Restrict)

  valuee    String
  valuee1      Valuee   @relation(fields: [valuee], references: [id], onDelete: Restrict)
}

